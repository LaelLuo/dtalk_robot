name: dtalk_robot
description: A starting point for Dart libraries or applications.
version: 1.0.0

environment:
  sdk: ^3.0.5

dependencies:
  crypto: ^3.0.1
  dio: ^5.4.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.16.0
